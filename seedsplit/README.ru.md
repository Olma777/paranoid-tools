[English](README.md) · **Русский**

# seedsplit

Распределить секрет на доли (Shamir Secret Sharing) — без снейкойла про «надёжность».

[![CI](https://github.com/Di-kairos/paranoid-tools/actions/workflows/ci-seedsplit.yml/badge.svg)](https://github.com/Di-kairos/paranoid-tools/actions/workflows/ci-seedsplit.yml)
![License: MIT](https://img.shields.io/badge/license-MIT-green)
![platform](https://img.shields.io/badge/platform-macOS-blue)
![windows](https://img.shields.io/badge/Windows-beta-orange)
![shellcheck](https://img.shields.io/badge/shellcheck-passing-brightgreen)

Часть экосистемы [Paranoid Tools](../README.md).

## Зачем это

Разбить seed-фразу, пароль или ключ на N долей так, что любые T из них восстанавливают
секрет, а T-1 долей не дают о нём **ничего**. Чтобы один носитель или бэкап не был
единой точкой отказа и компрометации: потеряли один лист — секрет цел; нашли один
лист — узнать ничего нельзя.

## Установка

Установщик тянет бинарь **и `SHA256SUMS` с релизного тега** (а не с подвижной ветки
`main`) и проверяет контрольную сумму **до** установки — fail-closed при любом
несовпадении.

### Verify-then-run (не доверяй — проверяй)

`curl | bash` означает запуск кода, который ты не читал. Лучше так — скачать, сверить
сумму, прочитать глазами, потом запустить:

```bash
base=https://github.com/Di-kairos/paranoid-tools/releases/download/seedsplit-v0.5.8
curl -fsSLO "$base/install.sh"
curl -fsSLO "$base/SHA256SUMS"
curl -fsSLO "$base/SHA256SUMS.sig"
printf '%s\n' 'releases@paranoid-tools namespaces="file" ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIH9DVd0vNOwa5hyr9gShaCWoNOVnUsrdHVO/WE0wCZkT' > allowed_signers
ssh-keygen -Y verify -f allowed_signers -I releases@paranoid-tools -n file -s SHA256SUMS.sig < SHA256SUMS &&   # подлинность: Ed25519, пришитый ключ
shasum -a 256 -c SHA256SUMS --ignore-missing &&   # целостность: сам install.sh
less install.sh &&                               # прочитать — и только потом:
bash install.sh
```

### Установка одной строкой

```bash
curl -fsSL https://github.com/Di-kairos/paranoid-tools/releases/download/seedsplit-v0.5.8/install.sh | bash
```

> **Целостность ≠ подлинность (честные границы).** Контрольная сумма доказывает, что
> скачанный бинарь совпадает с `SHA256SUMS` из **того же релиза** — это ловит
> повреждение, частичную/кэш-подмену и не даёт запустить код с подвижной ветки `main`.
> Подлинность даёт подпись Ed25519 над `SHA256SUMS`: её проверяют и сниппет выше, и
> `install.sh` — по ключу, пришитому в этом репо; без проверки установщик отказывает
> (см. `SECURITY.md`). Остаточный риск — один проектный ключ на все пять тулов, см.
> [модель угроз](../THREAT-MODEL.ru.md). Для
> воспроизводимости фиксируй версию: `SEEDSPLIT_VERSION=0.5.8` вместо `latest`. Источник
> можно переопределить через `SEEDSPLIT_BASE_URL`, путь установки — через `SEEDSPLIT_DEST`.

### Язык вывода

По умолчанию английский. Для русского — `ST_LANG=ru` (русская системная локаль
подхватывается автоматически).

## Использование

Секрет читается из **stdin** или `--file`, **никогда** из argv (argv виден в `ps`).

Разбить секрет на 5 долей с порогом 3 (любые 3 восстановят, любые 2 не дадут ничего):

```bash
$ printf '%s' "legal winner thank year wave sausage worth useful legal winner thank yellow" \
    | seedsplit split -n 5 -t 3
SSS3-b552023e-3-1-5b5a5efd9de59461…-c31a70e8-48d2
SSS3-b552023e-3-2-ed9f531596633fc8…-5f2d94ab-2f21
SSS3-b552023e-3-3-e3c529846ee1cac5…-80b6e21c-f18f
SSS3-b552023e-3-4-9889f085f80357f1…-ea44f907-311f
SSS3-b552023e-3-5-96d38a140081a2fc…-12c8d35e-b91b
```

Каждая строка — самостоятельная доля формата
`SSS3-<setid>-<T>-<x>-<hexY>-<parity>-<chk4>`: версия формата, случайный **set-id**, общий для
всех долей одного сплита (поэтому `combine` детерминированно отказывается смешивать доли из
*разных* сплитов), порог, номер точки, тело, поле **parity** (Рид—Соломон) и 4-символьная
контрольная сумма. Разнеси строки по разным носителям/местам.

**Коррекция опечаток (с 0.5.0).** Доли живут на бумаге, а бумагу переписывают руками. Поле
parity — Рид—Соломон над тем же GF(256), что и само разбиение: `combine` чинит **до двух байт
с опечаткой на долю** — хоть в теле, хоть в самом поле parity — и сообщает об этом после того,
как секрет сошёлся.

Что происходит дальше двух байт — точная формулировка: у кода расстояние 5, поэтому сильно
испорченная доля может «декодироваться» к ДРУГОМУ кодовому слову, а не просто провалиться.
Такой кандидат отбрасывается контрольной суммой доли, а если и она сошлась — 128-битным tag'ом
нагрузки. То есть `combine` по-прежнему отдаёт либо точный секрет, либо ошибку, но гарантию
дают эти проверки, а не отказ самого декодера. Чего parity не кроет вовсе: короткую
структурную голову (`setid`, `T`, `x`) — там опечатку ловит контрольная сумма, но не чинит;
и пропущенные/лишние символы, сдвигающие всё за собой. Такую долю надо переписать заново.

Доли, распечатанные версией 0.4.x (`SSS2-…`, без поля parity), собираются по-прежнему — просто
чинить их нечем. Миграции нет: нужна коррекция — разбей секрет заново.

Восстановить секрет — подай **любые T (или больше) долей** на stdin, по одной на строку:

```bash
$ head -3 shares.txt | seedsplit combine
legal winner thank year wave sausage worth useful legal winner thank yellow
```

Можно подать доли и файлами-аргументами:

```bash
seedsplit combine share1.txt share2.txt share3.txt
```

Проверить, что набор восстанавливается, **не раскрывая секрет** (удобно при раскладке
долей по носителям — подтвердить корректность, ничего не показывая). Выходит с кодом `0`
и печатает только длину восстановленного секрета, но не сам секрет:

```bash
$ head -3 shares.txt | seedsplit verify
✓ verify: доли согласованы, секрет восстановим (36 байт). Секрет НЕ показан.
```

Версия и справка:

```bash
seedsplit version        # или -v / --version
seedsplit help           # или -h / --help
```

### Команды и флаги

| Команда | Что делает |
|---|---|
| `seedsplit split [-n N] [-t T] [-p] [--file F]` | Разбить секрет (из stdin или `--file`) на `N` долей; любые `T` восстанавливают его. По умолчанию `-n 3 -t 2`. |
| `seedsplit split -p` / `--passphrase` | Сначала зашифровать секрет (openssl AES-256-CBC + PBKDF2, с 16-байтовым sha256-тегом внутри шифротекста), чтобы собранный порог всё равно требовал passphrase — а неверный passphrase получал отказ, а не мусор. `combine` сам определит и спросит его. См. «Область применения и ограничения». |
| `seedsplit combine [FILE...]` | Восстановить секрет из ≥T долей (читает из stdin по строке на долю, либо из `FILE`). Спросит passphrase, если доли сделаны с `-p`. |
| `seedsplit verify [FILE...]` | Подтвердить, что ≥T долей восстанавливаются, **не печатая секрет** (печатает только длину восстановленного секрета). |
| `seedsplit version` | Показать версию (также `-v` / `--version`). |
| `seedsplit help` | Показать справку (также `-h` / `--help`). |

Ограничения параметров: порог `-t` должен быть ≥2 (иначе одна доля = весь секрет),
`-n` должно быть ≥ `-t` и ≤255 (точки оценки в GF(256)); секрет — до 65535 байт.

## Как это работает

Чистая реализация **Shamir Secret Sharing над GF(256)** (поле `2^8`, редуцирующий
многочлен `0x11b` как в AES; сложение = XOR, умножение через таблицы log/antilog от
генератора `0x03`). Случайные коэффициенты многочленов берутся из `/dev/urandom`.

Секрет оборачивается заголовком целостности (`0x55` | длина | секрет | **128-битный
tag** из sha256), поэтому `combine` возвращает **либо точный секрет, либо честный
отказ** — шанс молча вернуть *не тот* секрет ≈ 2⁻¹²⁸. Каждая доля также несёт случайный
**set-id** (4-байтный nonce, *не* выведенный из секрета — так что его нельзя использовать
как оракул подтверждения догадки), благодаря чему `combine` детерминированно отвергает
смесь долей из разных сплитов. Отказы различимы и конкретны: порченная доля (контрольная
сумма), доли из разных сплитов (set-id), ниже порога, несогласованный порог или
провалившаяся проверка целостности.

## Scope & limitations

Честность — смысл всей экосистемы; здесь особенно легко скатиться в снейкойл, поэтому
вот честные пределы:

- **Реализация GF(256) — наша собственная, и независимого аудита у неё НЕ БЫЛО.**
  Вместо молчания об этом — вот что есть: CI на каждый push дифференциально
  сверяет её со [второй реализацией, написанной с нуля](test/differential/) на
  другом языке (Python) по той же спецификации — тем же проектом. `split` здесь
  обязан собираться там и наоборот, на случайных векторах в обе стороны, а
  RS-parity каждой доли пересчитывается вторым кодером; плюс property-тесты
  (T−1 долей — отказ, любые T из N собирают, смешение сплитов — отказ,
  Reed-Solomon чинит до 2 опечаток на долю). Честно о том, что это доказывает:
  такая сверка ловит ошибки **реализации** (кривая таблица, сломанный Горнер),
  но не изъян в **самой схеме**, и сторонним ревью не является. Арифметика поля
  дополнительно заякорена на опубликованные внешние векторы — умножение и
  инверсия из FIPS-197, антилог-таблица генератора 3 по Rijndael — в
  [`kat_vectors.py`](test/differential/kat_vectors.py). Независимый аудит
  всё равно был бы лучше; пока его нет, считай эту строку границей доверия.
- **качество долей = качество ГСЧ** — берём `/dev/urandom`, не самопал;
- **секрет в `argv` виден в `ps`** — ввод только через stdin/файл, не аргументом;
- **доли безопасны ровно настолько, насколько безопасно ты их ХРАНИШЬ и РАЗНОСИШЬ;**
  порог защищает от утечки <T долей, но не от ПОТЕРИ ≥(N-T+1) долей — это твоя
  ответственность за географию и носители;
- **совместимости со SLIP-39 / аппаратными кошельками ПОКА НЕТ** — полноценный SLIP-39
  (1024-словарь + RS1024 + шифрование) — это отдельное решение по объёму, не «ноль
  зависимостей»; честно помечено как scope-решение на будущий пак;
- **умножение в GF(256) идёт через таблицы log/antilog и НЕ constant-time** — есть
  тайминговый side-channel. Он вне модели угроз этого инструмента (локальный CLI без
  удалённого или онлайн-оракула на `split`/`combine`), но мы его не скрываем.
- **слой passphrase (`-p`) использует openssl**, а не zero-dep ядро — это опциональное
  encrypt-before-split (AES-256-CBC + PBKDF2): собранный порог даёт лишь запечатанный
  контейнер, секрет по-прежнему защищён passphrase. Ядро `split`/`combine` остаётся без
  зависимостей; openssl вызывает только `-p` (есть по умолчанию на macOS/Linux). Windows-порт
  восстанавливает запечатанный контейнер и подсказывает расшифровать его конвейером
  `openssl enc -d`.
  **Контейнер аутентифицирован.** У `openssl enc` нет режима AEAD, а проверка дополнения
  PKCS#7 — это не проверка пароля: сама по себе она пропускает неверный passphrase с
  вероятностью чуть выше 1/256, и наружу уходит правдоподобный мусор вместо секрета. Поэтому
  внутрь шифротекста кладётся 16-байтовый sha256-тег секрета, а перед контейнером стоят пять
  ASCII-байт `SSPP1`. Неверный passphrase теперь не проходит тег (≈2⁻¹²⁸) и получает честный
  отказ — то же обещание «точный секрет либо отказ», что даёт слой Shamir.
  **Старые доли**, нарезанные до этого формата (голый контейнер `Salted__`), по-прежнему
  собираются, но `combine` предупреждает: старый формат не отличает неверный passphrase от
  верного. Пересоздай их через `split -p`, чтобы перейти на аутентифицированный формат.
  **Крайний случай:** незашифрованный секрет, случайно начинающийся с байтов `SSPP1` (≈2⁻⁴⁰)
  или `Salted__` (≈2⁻⁶⁴) — для произвольных данных, для реальных seed-фраз не проблема, —
  `combine` ошибочно примет за зашифрованный: вместо секрета получишь ошибку «неверный
  passphrase» (утечки нет); пересоздай с `-p` или расшифруй вручную.

## Архитектура

- Single-file Bash, ноль зависимостей. Shamir над GF(256) реализован на чистом Bash;
  ГСЧ — `/dev/urandom`. Тесты: **bats** (44 теста — `split`/`combine`/`verify`,
  round-trip по каждому пороговому подмножеству, полная таксономия отказов, 128-битный
  tag целостности, плюс known-answer тесты: GF-векторы FIPS-197 и замороженный набор
  долей).
- Общее ядро (`lib/common.sh`) **вендорится** inline из соседнего каталога
  `securetrash/` этого же монорепо, содержимое пиннуто по SHA256;
  `tools/vendor-common.sh --check` ловит дрейф в CI. См. [`paranoid-tools/README.md`](../README.md).

## Windows (beta)

PowerShell-порт уже существует — в [`windows/README.md`](windows/README.md). Он повторяет
логику macOS — тот же Shamir над GF(256), `RNGCryptoServiceProvider` вместо `/dev/urandom` —
и даёт **байт-совместимые** доли (раздели на одной ОС, собери на другой).

> **Beta:** Windows-порт протестирован по логике (Pester на CI), но ещё не проверен на
> реальном Windows-железе. См. [`windows/README.md`](windows/README.md).

## Лицензия

[MIT](LICENSE). Уязвимости — см. [SECURITY.md](SECURITY.md); как контрибьютить —
[CONTRIBUTING.md](CONTRIBUTING.md).

Программа предоставляется «как есть», без каких-либо гарантий. seedsplit делит секрет
математически корректно (порог Шамира), но **не отвечает** за то, как ты хранишь и
разносишь доли — это твоя ответственность.
