# RELEASE-STATE — Paranoid Tools

> Технический снимок версий/тегов/подписей. Раньше жил в `MANIFEST.md`;
> переименован, т.к. `MANIFEST.md` теперь занят манифестом движения (см. также `MANIFEST.ru.md`).

## Монорепо (2026-08-13, s37)

Пять тул-репозиториев слиты в этот репозиторий как subtree (история сохранена);
пер-тул репозитории **архивированы** — read-only, но их страницы, теги и релизные
ассеты продолжают отдаваться. Что из этого следует:

- **Версионирование остаётся независимым**, per tool. Схема тегов —
  `<tool>-vX.Y.Z` (`securetrash-v0.5.7`, …); голые `vX.Y.Z` в монорепо не живут,
  единственное исключение — уже существующий `gui-v0.1.0`.
- **Схема ассетов и URL (принята s37, вторая волна).** Релиз привязан к тегу,
  ассеты сохраняют ПЛОСКИЕ имена (`<tool>`, `install.sh`, `<tool>.ps1`,
  `install.ps1`, `SHA256SUMS`, `SHA256SUMS.sig`) — коллизий между тулами нет,
  потому что каждый URL включает тег. **`releases/latest` не используется
  нигде**: в монорепо это «последний релиз ЛЮБОГО тула», и
  `latest/download/install.sh` отдал бы ассет чужого инструмента. Все ссылки —
  только пиннутые: `releases/download/<tool>-vX.Y.Z/<asset>`.
- **Версия по умолчанию вшита в код и заперта гейтами.** Дефолт живёт в трёх
  местах — `<TOOL>_VERSION_DEFAULT` в `<tool>/install.sh` и
  `<tool>/windows/install.ps1`, карта `tool_version_for()` в умбреллном
  `install.sh` — плюс пин в `verify-releases.sh`. `release.yml` перед подписью
  проверяет, что все четыре равны версии тега (и что инсталлеры не качают из
  архивных репо); рассинхрон = релиз не выйдет.
- **Порядок нарезки релиза тула:** bump `VERSION` в скрипте и `.ps1` → запись в
  `CHANGELOG.md` (ссылка на монорепо-тег; исторические compare-ссылки на
  архивные репо не трогаем) → обновить все четыре дефолта выше → дождаться
  зелёного `ci-<tool>.yml` (release.yml Windows-матрицу не гоняет) → тег
  `<tool>-vX.Y.Z` → release.yml тестирует на коммите тега, подписывает и
  публикует → пост-релизный `chore(formula)`: URL формулы на tarball
  монорепо-тега + свежий sha256 (формула не может нести хеш тарбола, в котором
  лежит сама, — поэтому всегда после тега).
- **Гейт секрета:** `RELEASE_SIGNING_KEY` (тот же ed25519-ключ; офлайн-бэкап в
  securetrash vault) должен лежать в secrets ЭТОГО репозитория — руки Mr.Di.
  Без него release.yml честно падает, неподписанных релизов не бывает.
- **Старые релизы** (`v0.5.6` / `v0.1.14` / `v0.1.15` / `v0.1.18` / `v0.5.5`)
  остаются в архивных репозиториях и продолжают отдаваться — сохранённые у
  пользователей старые `install.sh` работают.
- **CI** — пер-тул workflow `ci-<tool>.yml` с paths-фильтром; бейджи в README
  тулов указывают на них. Maintainer-режим умбрелльного `install.sh` теперь
  только по `PT_DEV=1` (в монорепо исходники есть у каждого клона — автовключение
  убило бы verify-then-run для публики).
- Vendoring `lib/common.sh` идёт из соседнего каталога `securetrash/` этого же
  репо (SHA-пин содержимого сохранён), сеть больше не нужна.

## Gate C закрыт — пять тегов нарезаны (2026-08-28, s42)

Миграция в монорепо завершена по факту, а не по плану: пять релизов существуют,
подписаны и проверяются. `verify-releases.sh` даёт **5/5**.

| Тег | Коммит | Релиз |
|---|---|---|
| `securetrash-v0.5.7` | `58884e2` | ✅ подписан, 6 ассетов |
| `vaultwatch-v0.1.15` | `0a7d401` | ✅ подписан, 6 ассетов |
| `panic-v0.1.16` | `0a7d401` | ✅ подписан, 6 ассетов |
| `ghostdraft-v0.1.19` | `0a7d401` | ✅ подписан, 6 ассетов |
| `seedsplit-v0.5.6` | `0a7d401` | ✅ подписан, 6 ассетов |

Проверено вживую после нарезки: все install-URL из доков отдают `200`; чистый
`git clone` + `bash install.sh` ставит 5/5 из подписанных релизов и лаунчер, версии
установленного совпадают с пиннутыми.

**Что пошло не так и как чинилось (важно для следующей нарезки).** Первый заход по
пяти тегам на `0a7d401` дал 4/5: `release.yml` гоняет `bats` на `ubuntu-latest` для
любого тула, а сьют securetrash работает поверх настоящих `hdiutil`/`shasum` за
`require_macos` — его собственный `ci-securetrash.yml` не зря держит эту джобу на
`macos-latest`. На Linux упало 58 тестов из 117, при том что CI тула был зелёным.
Починено в `58884e2`: локальный прогон `bats` в `release.yml` теперь смотрит, на каком
раннере тул гоняет свой сьют (значение читается из `ci-<tool>.yml`, а не зашито по
имени), и пропускает дубль там, где раннер не ubuntu. Пропуск не ослабляет гейт:
шаг «`ci-<tool>.yml` должен быть зелёным ровно на этом коммите» обязателен и
покрывает сьют на правильном раннере. Тег `securetrash-v0.5.7` был переставлен на
`58884e2` (релиза под ним не существовало, ассеты никто не скачивал).

**Про paths-фильтр и push тега.** Ранее здесь стояло, что push тега не поднимает
`ci-<tool>.yml` «потому что тег не меняет файлов». Это неверно: GitHub считает
paths-фильтр по диффу коммита, на который указывает тег. `0a7d401` тронул каталоги
всех пяти тулов — прогоны поднялись сами. Правило точнее звучит так: если диff
коммита под тегом НЕ задевает каталог тула (типичный случай — правка только
`.github/`), прогона не будет, и его нужно поднять вручную:

```bash
gh workflow run ci-<tool>.yml --ref main   # дождаться зелёного, потом ставить тег
```

**Код впереди тегов — незакрытый долг (2026-09-07, s44).** Аудит `AUDIT_2026-09-07_MARKET.md`
закрыт кодом на восемь находок из девяти, но нарезки не было: исправления лежат в `[Unreleased]`
у **seedsplit** (F01 — неверный passphrase давал код 0 и чужие байты), **vaultwatch** (F03 —
фоновые задачи не стартовали без `-ExecutionPolicy Bypass`) и **panic** (F09 — замер длительности).
Практическое следствие: установка через brew или `install.sh` отдаёт seedsplit с тем самым
дефектом. Правки `verify-releases.sh` (F04) и трея (F02) тегами не покрываются — они репозиторные.
Третий кат — гейт Mr.Di; порядок нарезки ниже, push тегов по одному.

**Пять тегов за один push — событий не будет (2026-09-02, s43).** GitHub не создаёт push-событий,
если в одном `git push` больше ТРЁХ тегов: пять тегов уехали на remote, `release.yml` не поднялся
ни разу, релизов не появилось. Лечится перестановкой (релизов под тегами не было, ассеты никто не
качал): удалить теги на remote и пушить по одному. Ручного триггера у `release.yml` нет —
`workflow_dispatch` в нём не объявлен, push тега единственный вход.

```bash
git push origin --delete <tag>...            # только пока под тегом НЕТ релиза
for t in <tag>...; do git push origin "$t"; sleep 12; done
```

Порядок нарезки следующего релиза:

```bash
# 1. bump VERSION + четыре дефолта (см. «Порядок нарезки релиза тула» выше)
# 2. убедиться, что ci-<tool>.yml зелёный ровно на том SHA, куда встанет тег
# 3. тег и push
git tag "<tool>-vX.Y.Z" <sha> && git push origin "<tool>-vX.Y.Z"
bash verify-releases.sh    # ждём 5/5
# 4. пост-релизный chore(formula): tarball монорепо-тега + свежий sha256
```

Снимок состояния пяти инструментов экосистемы на момент последнего обновления.

> **Что это и чем НЕ является.** Это *convenience*-снимок (карта «какой тег у каждого
> тула сейчас»), а **не** строгий lock-файл. Инструменты версионируются независимо
> внутри монорепо. Рассинхрон этой таблицы с реальностью не ломает сборку/установку —
> он лишь означает, что снимок устарел. Обновлять при закрытии сессии вместе с `HANDOFF.md`.

Обновлено: 2026-09-02 (сессия 43 — второй кат пяти релизов: всё, что чинилось за сессию по
Windows, живёт в файлах, которые едут из подписанного релиза, а не из клона, — без тегов у
пользователя оставались версии с неработающим запуском, молчаливыми отказами по правам и
черновиком в «Блокноте». `verify-releases.sh` 5/5, формулы переведены на новые тарболы).

| Tool | Каталог | Tag (release) | Version | Статус |
|------|---------|---------------|---------|--------|
| securetrash | `securetrash/` | `securetrash-v0.5.8` | **0.5.8** | CI ✅ · Release подписан ✅ · vault отказывается в консоли без прав администратора вместо оставленного подключённого НЕЗАШИФРОВАННОГО тома; промпт пароля больше не выбрасывает набранное — короткий пароль это предупреждение с выбором, а не запрет; подтверждение сравнивается в неуправляемой памяти; названы MFT-резидентные копии |
| vaultwatch  | `vaultwatch/`  | `vaultwatch-v0.1.16` | **0.1.16** | CI ✅ · Release подписан ✅ · `start` fail-closed без прав (иначе сторож регистрировался и молча не срабатывал); обе задачи с `-RunLevel Highest` и без вспышки консоли раз в минуту; `--force` в TTL ожил (в задаче планировщика подтверждать было некому); счётчик VSS стал tri-state |
| panic       | `panic/`       | `panic-v0.1.17` | **0.1.17** | CI ✅ · Release подписан ✅ · без прав честно говорит, что открытый сейф останется открытым, вместо «заперто 0 томов»; чистит историю буфера (Win+V) и jump-списки; считает несохранённые вкладки «Блокнота» и прямо говорит, что не удаляет их |
| ghostdraft  | `ghostdraft/`  | `ghostdraft-v0.1.20` | **0.1.20** | CI ✅ · Release подписан ✅ · черновик больше не уходит в «Блокнот», который держит несохранённые вкладки на диске мимо нашего shred; по умолчанию консоль и ноль файлов, внешний редактор opt-in и ждёт человека перед уничтожением |
| seedsplit   | `seedsplit/`   | `seedsplit-v0.5.7` | **0.5.7** | CI ✅ · Release подписан ✅ · раскладка `lib\` в установщике: команда наконец запускается из PowerShell (`.ps1` в PATH перехватывал имя у `.cmd`-шима и падал на ExecutionPolicy) |

**Архивные релизы** (`v0.5.6` / `v0.1.14` / `v0.1.15` / `v0.1.18` / `v0.5.5`) остаются в
пяти заархивированных репозиториях и продолжают отдаваться — сохранённые у пользователей
старые `install.sh` работают. Новые ссылки ведут только на монорепо-теги.

**Homebrew.** Формулы в `<tool>/Formula/<tool>.rb` качают тарбол монорепо-тега и ставят
`<tool>/<tool>` (в архиве скрипт лежит уровнем ниже). Публичный tap `Di-kairos/homebrew-tap`
несёт только `securetrash` (0.5.7) — остальные четыре ждут решения Mr.Di.

> Формулы Homebrew в репозиториях обновлены на новые теги + sha256 (source-тарбол); в tap
> опубликован только `securetrash` (на `v0.5.6`), остальные четыре — гейт Mr.Di.
> `verify-releases.sh` пиннут на эти теги. HEAD-колонка убрана — дрейфует; смотри
> `git -C <tool> describe --tags`.

> **Дрейфа нет на 2026-08-08 (s33).** Пять катов за сессию — все по одному дефекту, который
> нашёл живой пользователь на русской Windows: `.ps1` лежали в UTF-8 без BOM, Windows
> PowerShell 5.1 читает такой файл в системной ANSI-кодировке (CP1251), кириллица в исходнике
> становилась мозаикой, кавычки внутри строк рвались — скрипт падал с ParserError до первой
> команды. Не работало НИЧЕГО: ни тул, ни установщик, ни лаунчер. CI этого не видел, потому что
> гонял только `pwsh` 7 (он читает UTF-8 по умолчанию), — целый класс дефектов был вне тестов.
> BOM проставлен во все 24 `.ps1`; `windows/test/encoding.Tests.ps1` падает, если хоть один его
> потеряет; CI каждого репозитория теперь ПАРСИТ и ГОНЯЕТ тесты под самим Windows PowerShell 5.1
> и делает smoke-запуск. Первый же прогон под 5.1 вычистил ещё пять расхождений (`$IsWindows`
> только с PS6, `Join-Path` больше двух сегментов только в PS7, у одиночного `[pscustomobject]`
> нет `.Count`, 5.1 теряет пустой аргумент нативной команды, трей ссылался на .NET-Core-сборку и
> вываливал C#-ошибку — теперь честно просит pwsh 7). `verify-releases.sh` **5/5** на новых тегах,
> BOM проверен в опубликованных ассетах, CI ×6 green, smoke-test 16 ✓ / 0 ✗ / 3 ручных.

> **Дрейфа нет на 2026-08-06 (s31).** У всех пяти тулов HEAD = тег + `chore(formula)` — норма.
> За сессию вышло ЧЕТЫРЕ ката. Первый довыпустил Windows-паритет детекта тома в `vaultwatch`.
> Второй прошёл по всем пяти: подписанные данные уходили верификатору через конвейер PowerShell,
> который дописывает свой перевод строки (82 байта там, где подписан 81) — у `seedsplit` это
> означало, что установить его на Windows с настоящего релиза было **нельзя вообще**; плюс
> перенаправленные потоки верификатора никто не вычитывал (болтливый `ssh-keygen` вешал
> установку намертво), а выход верификатора до чтения stdin ронял установщик исключением вместо
> честного «подпись не сошлась». Все три воспроизведены тестами. Тем же катом: установщик тула
> больше не кладёт вторую копию рядом с установкой из умбреллы, и `panic hotkey` на Windows
> отвечает причиной вместо «Unknown command».
> Третий кат (`ghostdraft`) — по живому тесту: из редактора нельзя было ни выйти, ни писать.
> Четвёртый — по финальному adversarial-проходу, и оба его дефекта были СТАРЫЕ: индексация тома
> оставалась выключенной навсегда после штатного `vault close` (восстановление невозможно на
> размонтированном томе, а сессия удалялась — теперь она хранится как долг и гасится при
> следующем открытии), и установщик Windows падал под штатным PowerShell 5.1
> (`ProcessStartInfo.ArgumentList` есть только в .NET Core).
> Лаунчер, трей и бар едут вместе с умбреллой (она тегов не носит).

Все пять инструментов — open source, опубликованы публично (исходники и релизы открыты).

## Release drift — закрыт

Все тулы перевыпущены (re-release 2026-06-24): между прошлым тегом и `main` накопились
install.sh/release.yml hardening (fail-closed) и фичи (`status` у vaultwatch/panic) — теги
не содержали их, хотя README документировал. Догнали: bump версий → CHANGELOG → теги →
релизы собраны и **подписаны** Ed25519-ключом (`SHA256SUMS.sig`, провалидировано end-to-end
через `verify-releases.sh`) → `sha256` в `Formula/*.rb` пере-синкнут под новые tarball'ы.
HEAD каждого тула = тег + `chore(formula)` + `docs`-bump версий в README (всё ПОСЛЕ тега —
это норма, не дрейф: формула и доки пере-синкаются под уже выпущенный релиз). Релизные
артефакты соответствуют коду.

## Release signing

Все 5 тулов подписывают `SHA256SUMS` ключом `releases@paranoid-tools` (Ed25519); pubkey
`ssh-ed25519 …CZkT` опубликован в каждом `SECURITY.md`, вшит в `install.sh` (авто-verify).
Приватный ключ — в GH Secrets (`RELEASE_SIGNING_KEY`) + офлайн-бэкап в securetrash vault
(`/Volumes/SecretVault/releases_paranoid_tools`, без парольной фразы — release.yml работает
без интерактива).

**Ротация 2026-08-26 (s41).** Секреты в GitHub привязаны к репозиторию и не переезжают:
`RELEASE_SIGNING_KEY` остался в пяти архивных репо, а в монорепо его не было — файл старого
ключа (`…scn2U`) не нашёлся ни в `~/.ssh`, ни в сейфе, а прочитать секрет из GitHub нельзя.
Выпущен новый ключ (`SHA256:rTzQ4loEpdy9/i06HtsV4wuQU8iMpprFFGuY+r4IPxk`), pubkey заменён во
всех установщиках, `SECURITY.md`, README и `verify-releases.sh`. Старые релизы в архивных
репо продолжают проверяться старым ключом из своих же `install.sh` — там ничего не сломалось;
монорепо-релизов CLI на момент ротации не существовало, так что подменять нечего.

## GUI signing (macOS — закрыто 2026-08-09, s34)

Apple Developer Program оплачен и активен → `ParanoidBar.app` **подписан и нотаризован**.
Identity `Developer ID Application: Daniel Diamant (TA24A89R8H)` (Xcode → Accounts → Manage
Certificates), notary-профиль в keychain — `paranoid-notary` (`xcrun notarytool
store-credentials`, app-specific password). Сборка релиза:

```bash
cd gui/macos && ./build.sh --bundle \
  --sign "Developer ID Application: Daniel Diamant (TA24A89R8H)" \
  --notarize paranoid-notary --dmg --version 0.1.0
```

Проверено независимо от вывода скрипта: `spctl --assess` → `accepted, source=Notarized
Developer ID`; `stapler validate` → ok (тикет вшит, Gatekeeper проходит **офлайн**);
`codesign -dvv` → `flags=0x10000(runtime)`, secure timestamp, `TeamIdentifier=TA24A89R8H`;
`codesign --test-requirement="=notarized"` → satisfied. Submission `315fba5f-edce-456e-82ff-4cd6ec217810`,
status `Accepted`.

**`.dmg` — 2026-08-10, s34.** `--dmg` собирает сжатый образ (`hdiutil`, UDZO) с `.app` и
симлинком на `/Applications`, подписывает и нотаризует его **отдельным заходом**: Gatekeeper
проверяет образ как самостоятельный артефакт при открытии, тикет вложенного `.app` этого не
покрывает. Submission `17b23554-273e-4a6d-9018-3d285eb9af27`, Accepted; `ParanoidBar-0.1.0.dmg`,
105 KB. Проверено монтированием готового образа: внутри `.app` со стейпленным тикетом
(`spctl` accepted, `stapler validate` ok, `=notarized` satisfied) плюс симлинк для drag-n-drop.

**Окно образа — 2026-08-10, s35.** Раскладку тома (icon view 640×400, иконки 128px, фон
`gui/macos/dmg-background.tiff` со стрелкой на симлинк) хранит `.DS_Store`, а пишет его только
Finder — поэтому `--dmg` собирает сначала UDRW, монтирует, оформляет через Automation и лишь
затем жмёт в UDZO. Готовый UDZO read-only, оформить его задним числом нельзя. Без
Automation-доступа (CI, ssh) шаг печатает предупреждение, образ остаётся валиден. Перед
отмонтированием с тома удаляется `.fseventsd` — журнал FS-событий незачем раздавать с образом.

Между монтированием и раскладкой стоит ожидание (s36): Finder узнаёт о томе через
DiskArbitration, а `hdiutil attach` возвращается раньше — на MacBook Pro M5 Max раскладка
успевала спросить Finder до появления тома и падала с `-1728` («Can't get disk»). Наружу это
выглядело как отказ Automation: образ подписан, нотаризован и валиден, но неоформлен. Ждём
появления тома именно у Finder — `/Volumes/ParanoidBar` к этому моменту уже существует и ничего
не доказывает.

**Иконка `.app` — 2026-08-10, s36.** До неё `Info.plist` шёл без `CFBundleIconFile`, и в Finder
с окном образа висела дефолтная заглушка — на 128px особенно заметная. Знак не рисовался заново:
взят терминальный квадрат с шевроном-промптом из `assets/logo.svg` (поле чернил `#0E0E10`,
фарфоровая монолиния `#F2EFE8`, толщина линии 7/116 как в локапе). `gui/macos/ParanoidBar.icns`
несёт все размеры 16…1024 (@1x + @2x), каждый отрисован из вектора, а не даунскейлом с 1024 —
на 16px иначе исчезает линия (в генераторе стоит пол в 1.25px). Проверено не тем, что лежит в
`Resources`, а тем, что отдаёт LaunchServices (`NSWorkspace.icon(forFile:)`).

Бандл и `.dmg` — build-артефакты, в git не лежат (`.gitignore`), пересобираются из исходника.
Фон образа и `.icns` — единственные ассеты, они в git есть; растеризатора SVG на машинах нет,
оба нарисованы разово на CoreGraphics (генераторы, как и у `assets/logo.png`, не хранятся).
**Windows-трей не подписан:** Authenticode требует сертификат коммерческого CA — отдельная
покупка, Apple-аккаунтом не покрывается. Остаётся хвостом вместе с шимом под Windows.

## Vendoring pin

Все 4 тула (vaultwatch/panic/ghostdraft/seedsplit) вендорят `securetrash/lib/common.sh`
с пина `221f2c7` (SHA `348afdd5…e6a0`), офлайн-проверка через `tools/vendor-common.sh --check`.
